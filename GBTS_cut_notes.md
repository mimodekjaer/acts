# GBTS cut / selection notes

Observations made while optimizing the GPU GBTS chain that concern the
physics selection (cuts to add, remove or retune). None of these were applied;
the optimization work keeps the selection unchanged.

(empty so far)
